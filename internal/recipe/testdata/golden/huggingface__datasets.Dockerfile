# Generated by osspipeline. Do not edit.
# repo:     huggingface/datasets
# source:   ci
# evidence: .github/workflows/ci.yml:test
# evidence: .github/workflows/ci.yml
# evidence: leg matrix.test=unit of [unit integration]
# evidence: leg matrix.os=ubuntu-latest of [ubuntu-latest windows-latest]
# evidence: leg matrix.deps_versions=deps-latest of [deps-latest deps-minimum]
# evidence: .github/workflows/ci.yml:check_code_quality
FROM python:3.10-bookworm
ENV DEBIAN_FRONTEND=noninteractive LANG=C.UTF-8 CI=1

RUN apt-get update \
 && apt-get install -y --no-install-recommends \
      ca-certificates \
      curl \
      ffmpeg \
      git \
      make \
      pkg-config \
      unzip \
      xz-utils \
 && rm -rf /var/lib/apt/lists/*

ENV \
    PIP_CACHE_DIR=/cache/pip \
    PIP_DISABLE_PIP_VERSION_CHECK=1 \
    PYTHONDONTWRITEBYTECODE=1 \
    UV_CACHE_DIR=/cache/pip/uv

RUN if ! getent passwd 1000 >/dev/null 2>&1; then \
      getent group 1000 >/dev/null 2>&1 || groupadd -g 1000 ossp; \
      useradd -u 1000 -g 1000 -m -s /bin/bash ossp; \
    fi \
 && mkdir -p /cache/pip \
 && chown -R 1000:1000 /cache/pip \
 && mkdir -p /work && chown 1000:1000 /work

RUN d="$(python3 -c 'import sysconfig; print(sysconfig.get_paths()["data"])')" \
 && mkdir -p "$d" \
 && chown -R 1000:1000 "$d"

USER 1000:1000
WORKDIR /work
